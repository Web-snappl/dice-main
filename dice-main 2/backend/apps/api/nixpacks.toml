[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
# Fix dependency execution: install dev deps for build + legacy peer deps for conflicts
cmds = ["npm install --include=dev --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start:prod"
